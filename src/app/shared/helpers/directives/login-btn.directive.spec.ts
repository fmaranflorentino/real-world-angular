import { LoginBtnDirective } from './login-btn.directive';

describe('LoginBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginBtnDirective();
    expect(directive).toBeTruthy();
  });
});
